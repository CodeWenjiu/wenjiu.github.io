theme = "terminus"
# The URL the site will be built for
title = "home"
base_url = "https://codewenjiu.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "monokai"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether footnotes are rendered in the GitHub-style (at the bottom, with back references) or plain (in the place, where they are defined)
bottom_footnotes = true

# When set to "true", support for GitHub-style alerts, a.k.a. callouts or admonitions, is enabled in the Markdown parser.
github_alerts = true

[extra]
close_responsive_menu_on_resize = true
copy_button = true
layout = "center"
favicon_emoji = "🤔"
show_default_author = true

main_menu = [
    { name = "blog", url = "blog", trailing_slash = true },
    { name = "projects", url = "projects", trailing_slash = true },
    { name = "github", url = "https://github.com/CodeWenjiu/codewenjiu.github.io", new_tab = true },
]

socials = [
    { name = "email", url = "mailto:2784307979@qq.com" },
    { name = "github", url = "https://github.com/CodeWenjiu" },
]

[extra.content_security_policy]
enable = true

# 关键：这是一个数组，每一项必须包含 directive 和 domains
allowed_domains = [
    { directive = "script-src", domains = ["'self'", "'unsafe-inline'", "'unsafe-eval'", "'wasm-unsafe-eval'"] },
    { directive = "connect-src", domains = ["'self'"] },
    { directive = "style-src", domains = ["'self'", "'unsafe-inline'"] },
    { directive = "img-src", domains = ["'self'", "data:", "blob:"] }
]
